// This is a comment, and is ignored by the compiler

// This is the main function
fn main() {
    // Statements here are executed when the compiled binary is called

    // Print text to the console
    let test = 1 + 1;
    let test2 = 2 * 2;
    println!("Hello World!");
    println!("Cool, another line!");
    println!("test ({type1}) (1 + 1) == {0} and test2 ({type2}) (2 * 2) == {1}", test, test2, type2="multiplication", type1="addition");
}